name: Generate Snake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest # This will run on Node.js 16

    steps:
      - uses: actions/checkout@v2

      - name: Generate the snake
        uses: Platane/snk@v2
        id: snake-gif
        with:
          github_user_name: Arshad-asd
          # You cannot use 'gif_out_path' and 'svg_out_path' as inputs. Use 'outputs' instead.
          outputs: |
            gif_out_path: dist/github-contribution-grid-snake.gif
            svg_out_path: dist/github-contribution-grid-snake.svg

      - run: git status

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: output
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
